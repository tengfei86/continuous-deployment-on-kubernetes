apiVersion: v1
kind: Service
metadata:
  name: dspdmservice
spec:
  type: LoadBalancer
  selector:
    app: dspdmservice
    role: backend
    env: production
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
    nodePort: 32703
